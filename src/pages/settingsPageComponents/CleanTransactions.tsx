import styled from "styled-components"

export default function CleanTransactions() {
  return (
    <Background>
      <h1>LIMPAR TRANSAÇÕES</h1>
    </Background>
  );
}

const Background = styled.div`
  display: flex;
`